{
  "name": "automatic-form-validation-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Monorepo: @manufosela/form-validators (pure predicates, MIT) + automatic_form_validation (auto-binding UI lib, Apache-2.0).",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "test": "pnpm -r --filter \"./packages/*\" run test",
    "test:coverage": "pnpm -r --filter \"./packages/*\" run test:coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/manufosela/Automatic-Form-Validation.git"
  },
  "author": "@manufosela",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/manufosela/Automatic-Form-Validation/issues"
  },
  "homepage": "https://github.com/manufosela/Automatic-Form-Validation#readme",
  "engines": {
    "node": ">=20"
  }
}
